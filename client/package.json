{
  "name": "phaser-discord-template",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:browser": "vitest run --project browser",
    "test:browser-debug": "vitest run --project browser --browser.headless=false",
    "test:unit": "vitest run --project unit",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.1.4",
    "vite": "^6.4.2"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^1.0.1",
    "@phaserjs/editor-scripts-base": "^2.0.1",
    "@phaserjs/editor-scripts-quick": "^2.0.0",
    "@testing-library/dom": "^10.4.1",
    "phaser": "^4.0.0",
    "playwright": "^1.59.1",
    "vitest": "^4.1.4"
  }
}
